name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH Deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            if [ ! -d ~/auto-appoinment-sytem ]; then
              git clone https://github.com/Rain-Great/auto-appoinment-sytem.git ~/auto-appoinment-sytem
            fi
            cd ~/auto-appoinment-sytem
            git pull origin main
            npm install --omit=dev
            mkdir -p data
            pm2 restart auto-appoinment-sytem || pm2 start app.js --name auto-appoinment-sytem
            pm2 save
